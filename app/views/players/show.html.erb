<h1>Players#show</h1>
<section>
  <%= @player.player_name %>
  <ul>
    <% @player.notes.reverse.each do |note| %>
      <li><strong><%= note.note_date %></strong>
      <br>
      <a href=<%= note.link_title %> target="_blank"><%= note.note_preview %></li></a>

    <% end %>
  </ul>
</section>
